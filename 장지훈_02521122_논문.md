# CrewAI 기반 계층적 멀티 에이전트를 활용한 자연어 로봇 제어 시스템

장지훈

과학기술연합대학원대학교 인공지능 전공

# Natural Language Robot Control System Using CrewAI-based Hierarchical Multi-Agent Architecture

Jihun Jang

Dept. of Artificial Intelligence, University of Science and Technology

*Corresponding author E-mail: thespringsong@gmail.com

---

## 요약

본 연구는 CrewAI 기반 멀티 에이전트 시스템을 활용한 자연어 로봇 제어 시스템을 제안한다. Planner, Executor, Validator 3개 에이전트가 협업하여 사용자의 자연어 명령을 UR10e 로봇의 안전한 동작으로 변환한다. Hierarchical planning 접근을 통해 계획과 실행을 분리하고, Function Calling과 Sequential Actions를 결합하여 구조화된 제어를 구현하였다. CrewAI BaseTool을 상속받아 4개의 커스텀 도구를 개발하였으며, PositionSensor를 통한 실시간 피드백과 안전성 검증 메커니즘을 통합하였다. Webots 시뮬레이터에서 실험한 결과, 기본적인 조작 명령을 성공적으로 처리할 수 있음을 확인하였다.

**키워드**: LLM 기반 로봇 제어, 멀티 에이전트 시스템, CrewAI, UR10e, Function Calling, Webots 시뮬레이션

---

## 1. 서론

### 1.1 연구 배경

로봇 제어 분야에서 자연어 인터페이스는 사용자 접근성을 크게 향상시킬 수 있는 핵심 기술이다. 그러나 전통적인 rule-based 방법은 사전에 정의된 명령어 집합에 한정되어 확장성과 유연성이 부족하다. 최근 Large Language Model(LLM)의 발전으로 자연어 이해 능력이 비약적으로 향상되었으나, 단순히 LLM을 적용하는 것만으로는 로봇 제어의 안전성과 신뢰성을 보장하기 어렵다.

로봇 조작에서는 물리적 제약, 충돌 회피, 관절 한계 등 다양한 안전 규칙을 준수해야 한다. 또한 추상적인 자연어 명령을 정밀한 관절 각도 명령으로 변환하고, 순차적으로 실행하는 과정에서 오류를 감지하고 검증하는 메커니즘이 필요하다.

### 1.2 연구 목적 및 기여

본 연구는 이러한 문제를 해결하기 위해 CrewAI 프레임워크를 활용한 멀티 에이전트 기반 로봇 제어 시스템을 제안한다. 주요 기여는 다음과 같다.

첫째, Hierarchical planning 접근을 통해 작업 계획(planning)과 실행(execution)을 명시적으로 분리하고, 안전성 검증(validation) 단계를 독립적으로 구성하였다. 이는 Vision-Language-Action(VLA) 모델의 Hierarchical Models 중 Subtask-Based Planner 접근을 따른다[1].

둘째, CrewAI의 BaseTool을 상속받아 로봇 제어에 특화된 4개의 커스텀 도구를 개발하여 에이전트 간 효과적인 협업을 가능하게 하였다.

셋째, Function Calling을 통한 구조화된 계획 생성과 Sequential Actions를 결합하여 안정적인 제어 파이프라인을 구축하였다.

넷째, Webots 시뮬레이터와 UR10e 로봇 모델을 활용하여 실제 적용 가능성을 검증하였다.

---

## 2. 관련 연구

### 2.1 VLA 모델과 Hierarchical Planning

Shao 등[1]은 Vision-Language-Action(VLA) 모델을 체계적으로 분류하였으며, 크게 Monolithic 모델과 Hierarchical 모델로 구분하였다. Monolithic 모델은 단일 시스템에서 지각부터 행동까지 end-to-end로 처리하는 반면, Hierarchical 모델은 계획과 실행을 명시적으로 분리한다.

특히 Hierarchical Models 중 Subtask-Based Planner Only 접근은 LLM이 고수준 계획(high-level planning)만 담당하고, 실제 제어는 별도의 저수준 제어기(low-level controller)가 수행한다. 이는 해석 가능한 중간 표현(interpretable intermediate representations)을 생성하여 디버깅과 안전성 검증을 용이하게 한다.

본 연구는 이러한 Hierarchical 접근을 따르며, 계획(Planner), 실행(Executor), 검증(Validator)을 독립적인 에이전트로 구성하여 역할을 명확히 분리하였다.

### 2.2 CrewAI 프레임워크

CrewAI는 역할 기반 에이전트 협업을 지원하는 멀티 에이전트 프레임워크이다. 각 에이전트는 명확한 역할(role), 목표(goal), 배경지식(backstory)을 가지며, 도구(tools)를 활용하여 작업을 수행한다. Sequential process를 통해 에이전트 간 순차적 협업이 가능하며, Task 단위로 작업을 정의하고 컨텍스트를 전달할 수 있다.

---

## 3. 시스템 설계

### 3.1 전체 아키텍처

본 시스템은 그림 1과 같이 3개의 에이전트가 순차적으로 협업하는 구조로 설계되었다. 사용자의 자연어 명령이 입력되면 Planner Agent가 작업을 분해하고, Executor Agent가 구체적인 로봇 명령으로 변환하며, Validator Agent가 안전성을 검증한 후 실행 여부를 결정한다.

```
[사용자 입력]
      ↓
[Planner Agent] ← get_robot_state, get_preset_poses
      ↓
[Executor Agent] ← validate_joint_angles
      ↓
[Validator Agent] ← validate_plan_safety
      ↓ (APPROVED)
[Command Queue]
      ↓
[UR10e Robot]
```
**그림 1. 멀티 에이전트 시스템 아키텍처**

### 3.2 멀티 에이전트 구성

표 1은 각 에이전트의 역할과 사용하는 도구를 정리한 것이다.

| 에이전트 | 주요 역할 | 사용 도구 |
|---------|----------|----------|
| Planner | 사용자 요청을 단계별 작업으로 분해 | get_robot_state, get_preset_poses |
| Executor | 추상 계획을 관절 각도 명령으로 변환 | get_robot_state, validate_joint_angles, get_preset_poses |
| Validator | 실행 전 안전성 검증 및 승인/거부 | validate_joint_angles, validate_plan_safety, get_robot_state |

**표 1. 에이전트별 역할 및 사용 도구**

**Planner Agent**는 사용자의 자연어 명령을 받아 현재 로봇 상태를 확인하고, 목표를 달성하기 위한 고수준 작업 계획을 수립한다. 예를 들어 "물건을 집어서 옮겨줘"라는 명령에 대해 (1) 물체 위치로 이동, (2) 그리퍼 열기, (3) 하강, (4) 그리퍼 닫기, (5) 목표 위치로 이동, (6) 놓기와 같이 단계별로 분해한다.

**Executor Agent**는 Planner가 생성한 추상적인 계획을 UR10e 로봇이 실행 가능한 구체적인 명령으로 변환한다. 각 관절(shoulder_pan, shoulder_lift, elbow, wrist_1/2/3)의 목표 각도와 그리퍼 동작을 JSON 형식으로 생성한다. 관절 각도가 물리적 한계 범위 내에 있는지 validate_joint_angles 도구로 사전 검증한다.

**Validator Agent**는 최종 실행 전 전체 계획의 안전성을 종합적으로 검증한다. 관절 각도 범위, 충돌 위험, 동작 순서(예: 이동 중 그리퍼 닫기 금지) 등을 확인하고 APPROVED 또는 REJECTED를 결정한다.

### 3.3 제어 파이프라인

시스템의 제어 흐름은 다음과 같다.

**(1) Function Calling 기반 계획 생성**
OpenAI Responses API를 활용하여 produce_plan 함수를 호출한다. 입력으로 사용자 목표와 현재 로봇 상태를 제공하면, 다음과 같은 JSON 형식의 구조화된 계획을 생성한다.

```json
[
  {
    "action": "move_arm",
    "params": {
      "targets": {
        "shoulder_pan_joint": 0.5,
        "shoulder_lift_joint": -0.8,
        "elbow_joint": 1.2
      },
      "speed": 2.0,
      "duration": 0.3
    }
  },
  {
    "action": "control_gripper",
    "params": {"action": "open"}
  }
]
```

**(2) Sequential Actions 실행**
검증이 완료된 계획은 비동기 명령 큐(Command Queue)에 등록되며, threading을 통해 순차적으로 실행된다. 지원하는 3가지 동작 타입은 다음과 같다.

- move_joints: 관절을 목표 각도로 이동
- open_gripper: 그리퍼 열기
- close_gripper: 그리퍼 닫기

각 명령 실행 시 setPosition()과 setVelocity()를 통해 모터를 제어하고, 지정된 duration 동안 시뮬레이션 step을 진행한다.

### 3.4 커스텀 도구 구현

CrewAI의 BaseTool을 상속받아 4개의 도구를 구현하였다. 각 도구는 Pydantic BaseModel 기반 input schema를 정의하고 _run() 메서드에서 실제 기능을 구현한다.

**(1) GetRobotStateTool**
현재 로봇 상태를 JSON 형식으로 반환한다. PositionSensor를 통해 각 관절의 실제 각도를 읽고, 그리퍼 상태와 엔드이펙터 높이를 추정한다. 센서가 없는 경우 목표값(getTargetPosition)으로 fallback한다.

**(2) ValidateJointAnglesTool**
관절 각도가 물리적 한계 범위 [-3.14, 3.14] 내에 있는지 검증한다. JSON 문자열로 관절-각도 쌍을 입력받아 각 값을 검사하고, 위반 사항이 있으면 구체적인 오류 메시지를 반환한다.

**(3) ValidatePlanSafetyTool**
전체 실행 계획의 안전성을 종합적으로 검증한다. 검증 규칙은 다음과 같다.
- 규칙 1: 이동 직후 그리퍼를 닫으면 안전하지 않음
- 규칙 2: 계획이 비어있지 않아야 함
- 규칙 3: 모든 관절 각도가 범위 내

**(4) GetPresetPosesTool**
미리 정의된 포즈(home, lift, down, reach_table, reach_shelf)를 조회한다. 자주 사용되는 안전한 자세를 프리셋으로 제공하여 계획 생성을 돕는다.

### 3.5 안전성 검증 메커니즘

시스템은 다층적 안전성 검증을 수행한다. Executor는 명령 생성 시 개별 관절 각도를 검증하고, Validator는 전체 계획의 논리적 일관성과 안전성을 확인한다. 초기화 단계에서는 NaN(Not a Number) 방지를 위해 홈 포지션으로 설정하고, 센서 안정화를 위해 10 step을 실행한다.

---

## 4. 구현

### 4.1 실험 환경

시스템은 다음 환경에서 구현 및 실험하였다.

- 시뮬레이터: Webots R2023b
- 로봇 모델: UR10e 6축 산업용 로봇팔
- LLM: OpenAI gpt-4o-mini (API 호출)
- 프레임워크: CrewAI 0.70.0, LangChain 0.3.0
- 프로그래밍 언어: Python 3.13

### 4.2 로봇 설정

UR10e 모델은 6개의 회전 관절(shoulder_pan_joint, shoulder_lift_joint, elbow_joint, wrist_1_joint, wrist_2_joint, wrist_3_joint)과 3핑거 그리퍼(finger_1/2/middle_joint_1)로 구성된다.

각 관절 모터는 초기화 시 홈 포지션으로 설정되며, PositionSensor가 활성화되어 실시간 각도 피드백을 제공한다. 센서가 없는 경우 getTargetPosition()으로 fallback하여 항상 유효한 상태 정보를 제공한다.

그리퍼는 velocity 모드로 동작하며, setVelocity()의 부호에 따라 열기(-) 또는 닫기(+)를 수행한다. 동작 후 속도를 0으로 설정하여 위치를 유지한다.

### 4.3 실행 예시

"캔을 집어서 옮겨줘"라는 명령에 대한 시스템 동작을 단계별로 설명한다.

**Step 1: Planner Agent**
현재 상태를 확인하고 작업을 6단계로 분해한다.
```
현재 상태: {
  "shoulder_lift_joint": -1.57,
  "elbow_joint": 1.57,
  "gripper": "closed"
}

계획:
1. 물체 위치로 이동
2. 그리퍼 열기
3. 하강하여 접근
4. 그리퍼 닫기
5. 목표 위치로 이동
6. 놓기
```

**Step 2: Executor Agent**
각 단계를 관절 각도 명령으로 변환한다.
```json
[
  {
    "action": "move_arm",
    "params": {
      "targets": {
        "shoulder_pan_joint": 0.5,
        "shoulder_lift_joint": -0.8,
        "elbow_joint": 1.2
      }
    }
  },
  {
    "action": "control_gripper",
    "params": {"action": "open"}
  },
  {
    "action": "move_arm",
    "params": {
      "targets": {"shoulder_lift_joint": -0.6}
    }
  },
  {
    "action": "control_gripper",
    "params": {"action": "close"}
  }
]
```

**Step 3: Validator Agent**
안전성 검증을 수행한다.
```
검증 항목:
✅ 관절 각도 [-3.14, 3.14] 범위 내
✅ 충돌 위험 없음
✅ 동작 순서 안전함
→ APPROVED
```

**Step 4: 실행**
Command Queue에 등록된 명령을 순차적으로 실행한다. 각 동작은 JSONL 형식으로 로깅된다.
```
1. move_joints → 0.3초
2. open_gripper → 0.25초
3. move_joints → 0.3초
4. close_gripper → 0.25초
```

### 4.4 로깅 시스템

모든 이벤트는 JSONL(JSON Lines) 형식으로 ur10e_run_logs.jsonl 파일에 기록된다. 각 로그 항목은 타임스탬프, 이벤트 종류, 세부 데이터를 포함한다.

```json
{"t": "2025-11-15T10:30:00Z", "kind": "state_snapshot", 
 "joints": {...}, "gripper": "closed"}
{"t": "2025-11-15T10:30:01Z", "kind": "exec_move", 
 "targets": {...}, "speed": 2.0}
{"t": "2025-11-15T10:30:02Z", "kind": "crew_approved", 
 "goal": "캔 집어서 옮겨줘", "plan": [...]}
```

---

## 5. 결과 및 논의

### 5.1 정성적 평가

본 시스템은 단순 조작 명령("물건 집기", "홈으로 이동", "팔 들어올리기")을 성공적으로 처리할 수 있었다. Hierarchical planning을 통해 계획-실행-검증이 명확히 분리되어, 각 단계에서 무엇이 수행되는지 추적 가능하였다.

CrewAI의 멀티 에이전트 구조는 역할 분리를 통해 시스템의 해석 가능성을 높였다. Planner의 고수준 계획, Executor의 저수준 명령, Validator의 검증 결과를 독립적으로 관찰할 수 있어 디버깅이 용이하였다.

Function Calling을 통한 구조화된 출력은 자연어의 모호성을 제거하고 안정적인 파싱을 가능하게 하였다. JSON 형식의 명령은 타입 안전성을 제공하며, 에러 처리가 명확하였다.

### 5.2 한계

본 연구는 다음과 같은 한계를 가진다.

첫째, 정량적 성능 평가가 이루어지지 않았다. 성공률, 평균 실행 시간, 안전 위반 빈도 등의 지표를 측정하는 자동화된 평가 시스템이 구현되지 않았다. 따라서 시스템의 신뢰성을 객관적으로 검증하기 어렵다.

둘째, 복잡한 작업에 대한 검증이 부족하다. 다단계 조작, 동적 환경 대응, 예외 상황 처리 등은 실험하지 못했다.

셋째, 센서가 PositionSensor로 제한되어 있다. 카메라를 통한 객체 인식, Force sensor를 통한 접촉 감지 등이 없어 실제 환경 적용에 제약이 있다.

넷째, 재계획 기능이 구현되지 않았다. 실행 중 실패가 발생하면 중단되며, 자동으로 대안을 모색하거나 리커버리 정책을 수행하지 못한다.

다섯째, API 비용과 지연 시간 문제가 있다. 매 명령마다 OpenAI API를 호출하므로 비용이 발생하고, 네트워크 지연으로 인해 실시간 반응이 어렵다.

---

## 6. 결론

본 연구는 CrewAI 기반 계층적 멀티 에이전트 시스템을 활용하여 자연어 로봇 제어의 기초적인 프레임워크를 구현하였다. Hierarchical planning 접근을 통해 계획과 실행을 분리하고 안전성 검증 단계를 독립적으로 구성함으로써, 해석 가능하고 디버깅 가능한 시스템을 제시하였다.

Function Calling과 Sequential Actions의 결합은 구조화된 제어를 가능하게 하였으며, CrewAI BaseTool을 활용한 커스텀 도구 구현은 에이전트 간 효과적인 협업을 지원하였다. Webots 시뮬레이터에서의 실험을 통해 기본적인 조작 명령 처리가 가능함을 확인하였다.

향후 연구 방향으로는 첫째, 카메라와 Force sensor 등 다중 센서 융합을 통한 환경 인식 능력 향상이 필요하다. 둘째, 실행 중 실패를 감지하고 자동으로 재계획하는 리커버리 메커니즘 구현이 요구된다. 셋째, 벤치마크 데이터셋을 활용한 정량적 성능 평가가 필요하다. 넷째, 로컬 LLM을 활용하여 비용과 지연 시간을 줄이는 최적화가 필요하다.

현재는 프로토타입 수준이나, 멀티 에이전트 기반 안전성 검증 프레임워크를 제시했다는 점에서 의의가 있다. 특히 Hierarchical planning의 해석 가능성과 역할 분리를 통한 모듈화는 향후 복잡한 로봇 제어 시스템 개발의 기반이 될 수 있을 것으로 기대된다.

---

## 참고문헌

[1] Rui Shao, Wei Li, Lingsen Zhang, Renshan Zhang, Zhiyang Liu, Ran Chen, Liqiang Nie, "Large VLM-based Vision-Language-Action Models for Robotic Manipulation: A Survey", arXiv preprint arXiv:2508.13073, 2025.

