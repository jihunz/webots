# LLM 기반 UR10e 로봇팔 제어 시스템

**장지훈 (02521122)**

---

## 1. 문제와 시나리오

### 문제 정의
- **과제 목표**: 자연어 명령을 안전하고 정확한 로봇 동작으로 변환
- **대상 로봇**: Webots UR10e 6축 산업용 로봇팔 + 3핑거 그리퍼

### 시나리오
**입력**: "캔을 집어서 옮겨줘"

**로봇 반응**:
1. Planner: 작업을 단계별로 분해
2. Executor: 관절 각도 명령 생성
3. Validator: 안전성 검증
4. 실행: 순차적 동작 수행

---

## 2. 시스템 아키텍처

### LLM 활용
- **API**: OpenAI gpt-4o-mini
- **구조**: ChatOpenAI 모델을 각 에이전트가 독립 호출

### 멀티 에이전트 (CrewAI)
```
사용자 입력
    ↓
┌─────────────────────────────────┐
│  Planner Agent                  │  고수준 작업 계획
│  - 목표 분석                     │
│  - 단계 수립                     │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│  Executor Agent                 │  실행 명령 변환
│  - 관절 각도 계산                │
│  - JSON 명령 생성                │
└─────────────────────────────────┘
    ↓
┌─────────────────────────────────┐
│  Validator Agent                │  안전성 검증
│  - 각도 범위 검사                │
│  - 충돌 회피 확인                │
│  - 승인/거부                     │
└─────────────────────────────────┘
    ↓
Command Queue → UR10e 실행
```

### 시뮬레이터 연동
- **Webots**: UR10e 모델, 6개 관절 모터
- **센서**: PositionSensor (실시간 각도 피드백)
- **제어**: 비동기 명령 큐 (threading)

---

## 3. 핵심 기법

### Function Calling
- **produce_plan 함수**: 구조화된 JSON 계획 생성
- **입력**: 사용자 목표 + 현재 로봇 상태
- **출력**: 
```json
[
  {"action": "move_arm", "params": {"targets": {...}}},
  {"action": "control_gripper", "params": {"action": "close"}}
]
```

### Sequential Actions
- **Command Queue**: 명령을 순차적으로 실행
- **3가지 동작**:
  - `move_joints`: 관절 이동
  - `open_gripper`: 그리퍼 열기
  - `close_gripper`: 그리퍼 닫기

### 멀티 에이전트
- **역할 분리**: Planner/Executor/Validator
- **도구 활용**: 4개 커스텀 BaseTool
  - `get_robot_state`: 상태 조회
  - `validate_joint_angles`: 각도 검증
  - `validate_plan_safety`: 계획 검증
  - `get_preset_poses`: 프리셋 조회


---

## 4. 데모

### 시나리오: "캔 집어서 옮겨줘"

#### Step 1: Planner
```
입력: "캔 집어서 옮겨줘"
현재 상태: {
  "shoulder_lift_joint": -1.57,
  "elbow_joint": 1.57,
  "gripper": "closed"
}

Planner 출력:
1. 물체 위치로 이동
2. 그리퍼 열기
3. 하강하여 접근
4. 그리퍼 닫기
5. 목표 위치로 이동
6. 놓기
```

#### Step 2: Executor
```json
[
  {
    "action": "move_arm",
    "params": {
      "targets": {
        "shoulder_pan_joint": 0.5,
        "shoulder_lift_joint": -0.8,
        "elbow_joint": 1.2
      }
    }
  },
  {
    "action": "control_gripper",
    "params": {"action": "open"}
  },
  {
    "action": "move_arm",
    "params": {
      "targets": {"shoulder_lift_joint": -0.6}
    }
  },
  {
    "action": "control_gripper",
    "params": {"action": "close"}
  }
]
```

#### Step 3: Validator
```
검증 항목:
✅ 관절 각도 [-3.14, 3.14] 범위 내
✅ 충돌 위험 없음
✅ 동작 순서 안전함
→ APPROVED
```

#### Step 4: 실행
```
Command Queue:
1. move_joints → 0.3초
2. open_gripper → 0.25초
3. move_joints → 0.3초
4. close_gripper → 0.25초

✅ 실행 완료
로그: ur10e_run_logs.jsonl에 기록
```

---

## 5. 결과/지표

### 현재 상태
- **구현 완료**: 기본 실행 파이프라인
- **로깅**: JSONL 형식으로 모든 이벤트 기록
  - `state_snapshot`: 상태 변화
  - `exec_move`: 이동 명령
  - `exec_gripper`: 그리퍼 동작
  - `crew_approved`: 계획 승인



### 정성적 관찰
- ✅ 단순 명령 처리 가능
- ⚠️ 복잡한 작업은 미검증
- ⚠️ 장시간 안정성 미확인

---

## 6. 한계와 개선

### 현재 한계

#### 1. 환각 대응
- **문제**: LLM이 불가능한 동작 제안 가능
- **현재**: Validator가 기본 안전 규칙만 검증
- **개선안**: 물리 시뮬레이션 기반 사전 검증

#### 2. 재계획 부재
- **문제**: 실패 시 중단됨
- **현재**: 에러 로깅만 수행
- **개선안**: 
  - 센서 기반 실패 감지
  - 리커버리 정책 구현
  - 자동 재계획

#### 3. 일반화 부족
- **문제**: 학습된 패턴 없음
- **현재**: 매번 LLM 호출 (비용/지연)
- **개선안**: 
  - 성공 사례 RAG 구축
  - Few-shot 학습
  - 로컬 LLM 미세조정

#### 4. 센서 한정
- **문제**: PositionSensor만 사용
- **개선안**: 
  - 카메라 기반 객체 인식
  - Force sensor 기반 접촉 감지
  - 다중 센서 융합

#### 5. 성능 최적화
- **문제**: 
  - API 비용 (호출당 과금)
  - 지연 시간 (네트워크 왕복)
- **개선안**: 
  - 로컬 LLM 활용
  - 캐싱 전략
  - 프리셋 우선 사용

---

## 요약

✅ CrewAI 멀티 에이전트 (Planner/Executor/Validator)  
✅ Function Calling 기반 계획 생성  
✅ Sequential Actions 실행  
✅ PositionSensor 피드백  
✅ 안전성 검증 (기본 규칙)  
✅ JSONL 로깅

**현실적 평가**: 프로토타입 단계, 실제 배포에는 추가 개발 필요

